# Sandbox for the filesystem wrap example

Point `examples/wrap_filesystem.json` at this directory (absolute path).

Warrant never needs a cloud API key for this MCP — the sensitive thing is
*which directories* the upstream process can see. Keep that list narrow.
